# 🕓 TimeZone Bot (Aiogram 3 + PostgreSQL + Docker)

Telegram-бот для конвертации времени между часовыми поясами. Поддерживает авторизацию пользователей через администратора.

---

## 🚀 Возможности

- Конвертация времени из Перми в другие города (по умолчанию — Москва)
- Обработка сообщений вида: `14`, `15:36`, `12.00`
- Авторизация пользователей через администратора:
  - При первом сообщении пользователя бот уведомляет админа
  - Админ выбирает: ✅ Авторизовать / ❌ Отклонить
- Использует PostgreSQL для хранения пользователей
- Полностью запускается через Docker

---

## 🐳 Быстрый запуск через Docker

### 🔧 1. Клонировать проект

```bash
git clone https://github.com/LelikCom/TimeZone_Bot.git
cd TimeZone_Bot
```

### 🔑 2. Настроить `.env`

Создай файл `.env` в корне проекта:

```env
BOT_TOKEN=токен_твоего_бота
ADMIN_ID=123456789
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/timezone_db
```

> `ADMIN_ID` — твой Telegram ID  
> `DATABASE_URL` — не меняй, если используешь docker-compose

### ▶️ 3. Запустить проект

```bash
docker-compose up --build
```

После запуска:
- Бот автоматически создаёт таблицу `users` в базе данных
- При первом сообщении от нового пользователя — админ получит инлайн-кнопки

---

## 🧱 Структура проекта

```
Timezone_bot/
│
├── bot/
│   ├── handlers/        # Обработка сообщений
│   ├── keyboards/       # Inline-кнопки
│   ├── models/          # SQL-запросы и схемы
│   ├── services/        # Логика конвертации и работа с БД
│   ├── config.py        # Чтение .env
│   ├── loader.py        # Инициализация
│   └── main.py          # Точка входа
│
├── .env                 # Переменные среды
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
└── README.md
```

---

## 💡 Полезные команды

```bash
docker-compose down           # Остановить контейнер
docker-compose up --build     # Перезапуск с пересборкой
docker-compose logs -f        # Просмотр логов
```

---

## 📜 Лицензия

Проект распространяется под лицензией MIT.  
Автор: [LelikCom](https://github.com/LelikCom)
